package com.example.client_internet;

import androidx.core.content.FileProvider;

public class fileprovider extends FileProvider {
    // 此处保留为空，因为 FileProvider 的实现通常不需要自定义代码。
    // FileProvider 需要在清单文件中定义一个内容提供程序类，
    // 但在大多数情况下，不需要在自定义的 FileProvider 类中添加额外的代码。
    // 如果有特殊需求，可以在这个类中添加自定义逻辑。
}